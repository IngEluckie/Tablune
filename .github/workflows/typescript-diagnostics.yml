name: Remove Temporary TypeScript Diagnostics

on:
  push:
    branches: [bootstrap/csv-mvp]

permissions:
  contents: write

jobs:
  cleanup:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: bootstrap/csv-mvp
      - name: Remove temporary TypeScript diagnostic files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/typescript-diagnostics.yml typescript-diagnostics.txt typescript-status.txt
          git commit -m "chore: remove temporary TypeScript diagnostics"
          git push origin HEAD:bootstrap/csv-mvp
